#include <string>

namespace controller
{
    void getUser(std::string url);
    void getUsers(std::string url);
    void createUser(std::string url);
    void updateUser(std::string url);
    void deleteUser(std::string url);
}